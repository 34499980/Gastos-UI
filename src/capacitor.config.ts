import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gastosAngular.app',
  appName: 'gastosAngular',
  webDir: 'dist/gastosAngular'
};

export default config;
